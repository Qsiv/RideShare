<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Notifications" >

		<!-- all of this went in RelativeLayout tag -->
<!--android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin" -->
    
   
        <TextView
            android:id="@+id/notifications_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentTop="true"
            android:background="#FF8800"
            android:typeface="serif"
            android:gravity="center"
            android:textStyle="bold"
            android:text="@string/notifications_title"
            android:textSize="24sp" />
    
    <ScrollView
    android:id="@+id/scroll"
    android:scrollbars="vertical"
    android:layout_below="@id/notifications_title"
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    android:background="#FFF8DC"
    >

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" >
            
       <TextView
        android:id="@+id/RSU_join_request"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="5dp"
        android:paddingBottom="5dp"
        android:textSize="18sp"
        android:text="@string/RSU_join_request" />
       
       <TextView
        android:id="@+id/ROU_accepted"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="5dp"
        android:textSize="18sp"
        android:text="@string/ROU_accepted" />
            
            
        </LinearLayout>

	</ScrollView>

</RelativeLayout>